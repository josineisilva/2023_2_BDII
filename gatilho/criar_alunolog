CREATE TABLE alunolog (
                      data       TIMESTAMP,
                      usuario    NAME,
                      operacao   CHAR(1),
                      matricula  INTEGER,
                      nome       CHAR(15)
                      );
